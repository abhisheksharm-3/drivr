import { Text } from "react-native"
import { SafeAreaView } from "react-native-safe-area-context"

const Rides = () => {
  return (
    <SafeAreaView className="flex items-center justify-center h-screen"><Text className="text-black">History</Text></SafeAreaView>
  )
}

export default Rides