import { Text } from "react-native"
import { SafeAreaView } from "react-native-safe-area-context"

const Home = () => {
  return (
    <SafeAreaView className="flex items-center justify-center h-screen"><Text className="text-black">Home</Text></SafeAreaView>
  )
}

export default Home