import { Text } from "react-native"
import { SafeAreaView } from "react-native-safe-area-context"

const SignIn = () => {
  return (
    <SafeAreaView className="flex items-center justify-center h-screen"><Text className="text-black">SignIn</Text></SafeAreaView>
  )
}

export default SignIn